import 'package:flutter/material.dart';
class selectedItems extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return
      Center(  child: Text('selectedItems'),)

    ;
  }
}
